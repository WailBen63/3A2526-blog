{# templates/home.twig #}
{# Page d'accueil publique - Conformité EF-ARTICLE-01, UX-RESPONSIVE, 2.1 Technologies #}

{% extends "base.twig" %}

{% block content %}
    <h1 class="mb-4">{{ page_title }}</h1>

    {# Démonstration d'interactivité avec Alpine.js (Conformité 2.1) #}
    <div x-data="{ open: false }" class="mb-5">
        <button @click="open = !open" class="btn btn-outline-primary btn-sm shadow-sm">
            <span x-show="!open">📩 Afficher le message de bienvenue</span>
            <span x-show="open">📤 Masquer le message</span>
        </button>
        <div x-show="open" x-transition class="alert alert-info mt-3 shadow-sm border-0">
            <strong>Bienvenue !</strong> Ce blog utilise une architecture MVC et un rendu Twig optimisé pour le mobile.
        </div>
    </div>

    {# Grille d'articles responsive (Conformité UX-RESPONSIVE-01/02) #}
    <div class="row g-4">
        {% for post in posts %}
            <div class="col-12 col-md-6">
                <div class="card h-100 shadow-sm border-0">
                    
                    {# Image à la une (EF-ARTICLE-03) #}
                    {% if post.image_une %}
                        <img src="/3A2526-Blog/public/uploads/articles/{{ post.image_une }}" 
                             class="card-img-top" alt="{{ post.titre }}"
                             style="height: 220px; object-fit: cover;">
                    {% else %}
                        <div class="bg-light text-center py-5 border-bottom">
                            <span class="text-muted">📷 Pas d'image</span>
                        </div>
                    {% endif %}
                    
                    <div class="card-body d-flex flex-column">
                        <h2 class="h5 card-title fw-bold">{{ post.titre }}</h2>
                        
                        {# Extrait nettoyé (EF-ARTICLE-01) #}
                        <p class="card-text text-secondary small">
                            {{ post.contenu|striptags|slice(0, 150) }}...
                        </p>
                        
                        {# Tags associés (EF-ARTICLE-04) #}
                        {% if post.tags %}
                            <div class="mb-3">
                                {% for tag in post.tags %}
                                    <span class="badge bg-secondary-subtle text-secondary border me-1">#{{ tag.nom_tag }}</span>
                                {% endfor %}
                            </div>
                        {% endif %}
                        
                        <a href="/3A2526-Blog/public/post/{{ post.id }}" class="btn btn-primary mt-auto">Lire l'article</a>
                    </div>
                    
                    <div class="card-footer bg-transparent border-top-0 text-muted small">
                        📅 Publié le {{ post.date_creation|date("d/m/Y") }} par <strong>{{ post.nom_utilisateur }}</strong>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12 text-center py-5">
                <div class="alert alert-light border">Aucun article publié pour le moment.</div>
            </div>
        {% endfor %}
    </div>
{% endblock %}