{# templates/base.twig #}
{# 
    Template de base (layout principal) - Structure commune à toutes les pages
    Implémente les fonctionnalités transversales exigées par le cahier des charges
    
    Caractéristiques principales :
    - Gestion des thèmes clair/foncé (UX-THEME-01/02)
    - Police dyslexique optionnelle (UX-ACCESSIBILITY-01)
    - Menu de navigation responsive avec authentification
    - Messages flash via gestionnaire de session
    - Intégration complète de Bootstrap 5 et Alpine.js
    
    @package Templates
    @conformité UX-THEME-01/02 : Sélecteur de thème clair/sombre persistant
    @conformité UX-ACCESSIBILITY-01 : Police dyslexique optionnelle
    @conformité EF-ACL : Menu adapté selon l'authentification et les rôles
    @conformité 2.2.1 : Gestionnaire de Session (messages flash)
    @conformité UX-RESPONSIVE-01/02 : Design mobile-first et responsive
    @conformité 2.1 Technologies : Bootstrap 5.X, Alpine.js, Twig 3.X
#}
<!doctype html>
<html lang="fr" 
      x-data="{ 
          theme: localStorage.getItem('theme') || (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'),
          isDyslexicFont: localStorage.getItem('dyslexicFont') === 'true' 
      }" 
      x-bind:data-bs-theme="theme">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    {# 
        Bootstrap 5 depuis CDN - Conformité 2.1 Technologies
        Intégrité SRI pour sécurité renforcée
    #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    
    {# 
        Police OpenDyslexic depuis Google Fonts
        Conformité UX-ACCESSIBILITY-01 : Police pour personnes dyslexiques
        Chargée conditionnellement mais toujours disponible
    #}
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=OpenDyslexic:400,700&display=swap">

    {# 
        Styles CSS pour la police dyslexique
        La classe .d-font force OpenDyslexic sur tout le contenu
        Important pour outrepasser les styles Bootstrap
        Conformité UX-ACCESSIBILITY-01 : Application complète de la police
    #}
    <style>
        .d-font * {
            font-family: 'OpenDyslexic', sans-serif !important;
        }
    </style>

    {# 
        Styles spécifiques pour le contenu des articles
        Améliore la lisibilité avec des espacements et formats cohérents
        Conformité UX : Expérience de lecture optimisée
    #}
    <style>
    .article-content {
        line-height: 1.6;
    }
    /* ... autres styles pour les articles ... */
    </style>
    
    {# 
        Alpine.js depuis CDN - Conformité 2.1 Technologies
        Micro-framework JavaScript pour l'interactivité frontend
        Déferré pour ne pas bloquer le rendu de la page
    #}
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>

    <title>{{ page_title|default('Mon Blog') }}</title>
</head>
<body class="d-flex flex-column min-vh-100" x-bind:class="{ 'd-font': isDyslexicFont }">

    {# 
        Barre de navigation principale
        Conformité UX-RESPONSIVE : Menu hamburger sur mobile
        Conformité EF-ACL : Menu adapté selon l'authentification
    #}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            {# Logo / Nom du blog #}
            <a class="navbar-brand" href="/3A2526-Blog/public/">Mon Blog POO</a>
            
            {# Bouton hamburger pour mobile #}
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            {# Contenu du menu #}
            <div class="collapse navbar-collapse" id="mainNavbar">
                
                {# Navigation principale #}
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link {{ url == '/' ? 'active' }}" href="/3A2526-Blog/public/">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ url == 'a-propos' ? 'active' }}" href="/3A2526-Blog/public/a-propos">À Propos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ url == 'contact' ? 'active' }}" href="/3A2526-Blog/public/contact">Contact</a>
                    </li>
                </ul>
                
                {# Actions utilisateur et contrôles #}
                <ul class="navbar-nav mb-2 mb-lg-0 d-flex flex-row gap-2">
                    
                    {# 
                        Bouton Thème Clair/Foncé - Conformité UX-THEME-01/02
                        Utilise Alpine.js pour la persistance dans localStorage
                        Changement immédiat sans rechargement de page
                    #}
                    <li class="nav-item">
                        <button 
                            @click="theme = (theme === 'dark' ? 'light' : 'dark'); localStorage.setItem('theme', theme)" 
                            class="btn btn-outline-light d-flex align-items-center"
                            aria-label="Basculer entre les thèmes clair et foncé"
                        >
                            <span x-show="theme === 'light'">☀️ Clair</span>
                            <span x-show="theme === 'dark'">🌙 Foncé</span>
                        </button>
                    </li>
                    
                    {# 
                        Bouton Police Dyslexique - Conformité UX-ACCESSIBILITY-01
                        Active/désactive la police OpenDyslexic
                        Persistance via localStorage
                    #}
                    <li class="nav-item">
                        <button 
                            @click="isDyslexicFont = !isDyslexicFont; localStorage.setItem('dyslexicFont', isDyslexicFont)" 
                            class="btn btn-outline-info d-flex align-items-center"
                            aria-label="Activer ou désactiver la police pour dyslexiques"
                        >
                            <span x-show="!isDyslexicFont">🔡 Standard</span>
                            <span x-show="isDyslexicFont">🅰️ Dyslexie</span>
                        </button>
                    </li>

                    {# 
                        Menu Utilisateur - Conformité EF-ACL
                        Affichage conditionnel selon l'authentification
                        Accès au tableau de bord selon les rôles
                    #}
                    {% if session.get('user_id') %}
                        {# Utilisateur connecté #}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown">
                                👤 {{ session.get('user_name') }}
                                {# Affichage du rôle principal #}
                                <small class="d-block text-muted">{{ session.get('user_role') }}</small>
                            </a>
                            <ul class="dropdown-menu">
                                {# 
                                    Accès tableau de bord pour Administrateurs et Éditeurs
                                    Conformité EF-ACL : Contrôle d'accès basé sur les rôles
                                #}
                                {% if session.get('user_role') in ['Administrateur', 'Éditeur'] %}
                                    <li><a class="dropdown-item" href="/3A2526-Blog/public/admin">📊 Tableau de Bord</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                {% endif %}
                                {# Option de déconnexion #}
                                <li><a class="dropdown-item text-danger" href="/3A2526-Blog/public/logout">🚪 Déconnexion</a></li>
                            </ul>
                        </li>
                    {% else %}
                        {# Utilisateur non connecté #}
                        <li class="nav-item">
                            <a class="nav-link text-white" href="/3A2526-Blog/public/login">🔐 Connexion</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>

    {# 
        Messages flash - Conformité 2.2.1 : Gestionnaire de Session
        Affichage temporaire des retours d'action
        Types : succès (vert) et erreur (rouge)
    #}
    {% if session.get('flash_success') %}
        <div class="alert alert-success alert-dismissible fade show m-3" role="alert">
            {{ session.get('flash_success') }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        {{ session.remove('flash_success') }}
    {% endif %}

    {% if session.get('flash_error') %}
        <div class="alert alert-danger alert-dismissible fade show m-3" role="alert">
            {{ session.get('flash_error') }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
        {{ session.remove('flash_error') }}
    {% endif %}

    {# 
        Contenu principal des pages
        Les templates enfants rempliront ce block
        flex-grow-1 : Prend tout l'espace disponible
    #}
    <main class="container mt-4 mb-5 flex-grow-1">
        {% block content %}{% endblock %}
    </main>

    {# 
        Pied de page
        mt-auto : Pousse le footer en bas grâce au flexbox
        Affichage de l'année courante et des technologies utilisées
    #}
    <footer class="mt-auto py-3 bg-light text-center">
        <div class="container">
            &copy; {{ "now"|date("Y") }} Mon Blog - Développé avec PHP 8, Twig et Bootstrap
        </div>
    </footer>

    {# 
        Bootstrap JavaScript bundle
        Nécessaire pour les composants interactifs (dropdowns, modals, etc.)
    #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>