{% extends "base.twig" %}

{% block content %}
    <article>
        <h1 class="mb-3">{{ post.titre }}</h1>  {# ← CORRIGÉ : post.titre #}
        <p class="text-muted border-bottom pb-3 mb-3">
            Publié le {{ post.date_creation|date("d/m/Y à H:i") }}  {# ← CORRIGÉ : post.date_creation #}
        </p>
        
        <div class="lead">
            {{ post.contenu|nl2br }}  {# ← CORRIGÉ : post.contenu #}
        </div>
    </article>

    <hr class="my-4">
    <a href="/3A2526-blog/public/" class="btn btn-secondary">&larr; Retour à l'accueil</a>
{% endblock %}